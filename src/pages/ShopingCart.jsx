import {
    FormList,
    FormInput,
} from './ShopingCartStyled';

export default function ShopingCart () {
    return (
            <FormList>Name:
            <FormInput>Input
            </FormInput>

            Email:
            <FormInput>Input
            </FormInput>

            Phone:
            <FormInput>Input
            </FormInput>

            Address:
            <FormInput>Input
            </FormInput>
            </FormList>
            
            );
}